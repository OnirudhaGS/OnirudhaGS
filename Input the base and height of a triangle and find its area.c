#include<stdio.h>
int main()
{
    float a,b;
    printf("Input the base of triangle: ");
    scanf("%f",&a);
    printf("Input the height of triangle: ");
    scanf("%f",&b);
    printf("Area of the triangle is: %f",.5*a*b);
}
